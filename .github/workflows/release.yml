name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Run tests
        run: go test ./...

  build:
    needs: test
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-22.04
            goos: linux
            goarch: amd64
            archive_ext: tar.gz
          - runner: ubuntu-22.04-arm
            goos: linux
            goarch: arm64
            archive_ext: tar.gz
          - runner: macos-15-intel
            goos: darwin
            goarch: amd64
            archive_ext: tar.gz
          - runner: macos-15
            goos: darwin
            goarch: arm64
            archive_ext: tar.gz
          - runner: windows-2025
            goos: windows
            goarch: amd64
            archive_ext: zip
          - runner: windows-11-arm
            goos: windows
            goarch: arm64
            archive_ext: zip

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Show build context
        shell: bash
        run: |
          set -euo pipefail
          echo "ref=${GITHUB_REF_NAME}"
          echo "runner=$(uname -a)"
          go version
          go env GOOS GOARCH CGO_ENABLED GOPATH GOMODCACHE GOCACHE

      - name: Run native tests
        run: go test ./...

      - name: Build archive
        shell: bash
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME}"
          dist="dist"
          binary="codegraph"
          binary_name="${binary}"
          if [ "${{ matrix.goos }}" = "windows" ]; then
            binary_name="${binary}.exe"
          fi
          archive_base="${binary}_${version}_${{ matrix.goos }}_${{ matrix.goarch }}"
          mkdir -p "${dist}/${archive_base}"
          GOOS="${{ matrix.goos }}" GOARCH="${{ matrix.goarch }}" CGO_ENABLED=1 go build -v -x -o "${dist}/${archive_base}/${binary_name}" ./cmd/codegraph
          cp README.md "${dist}/${archive_base}/README.md"
          cp LICENSE "${dist}/${archive_base}/LICENSE"
          if [ "${{ matrix.archive_ext }}" = "zip" ]; then
            (
              cd "${dist}/${archive_base}"
              zip -q -r "../${archive_base}.zip" .
            )
            if [ "${RUNNER_OS}" = "macOS" ]; then checksum="shasum -a 256"; else checksum="sha256sum"; fi
            ${checksum} "${dist}/${archive_base}.zip" > "${dist}/${archive_base}.zip.sha256"
          else
            tar -C "${dist}" -czf "${dist}/${archive_base}.tar.gz" "${archive_base}"
            if [ "${RUNNER_OS}" = "macOS" ]; then checksum="shasum -a 256"; else checksum="sha256sum"; fi
            ${checksum} "${dist}/${archive_base}.tar.gz" > "${dist}/${archive_base}.tar.gz.sha256"
          fi

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.goos }}-${{ matrix.goarch }}
          path: |
            dist/*.tar.gz
            dist/*.tar.gz.sha256
            dist/*.zip
            dist/*.zip.sha256

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Flatten downloaded artifacts
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p release-dist
          find dist -type f \( -name "*.tar.gz" -o -name "*.tar.gz.sha256" -o -name "*.zip" -o -name "*.zip.sha256" \) -exec cp {} release-dist/ \;

      - name: Extract release notes from CHANGELOG.md
        shell: bash
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME}"
          section_header="## ${version} -"

          awk -v header="$section_header" '
            BEGIN { found=0 }
            $0 ~ /^## / && found { exit }
            index($0, header) == 1 { found=1 }
            found { print }
          ' CHANGELOG.md > release-notes-full.md

          if ! grep -q "^## ${version} -" release-notes-full.md; then
            echo "Could not find release notes section for tag ${version} in CHANGELOG.md"
            exit 1
          fi

          tail -n +2 release-notes-full.md > release-notes.md

      - name: Create or update GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail

          if gh release view "${GITHUB_REF_NAME}" >/dev/null 2>&1; then
            gh release edit "${GITHUB_REF_NAME}" \
              --title "${GITHUB_REF_NAME}" \
              --notes-file release-notes.md
          else
            gh release create "${GITHUB_REF_NAME}" \
              --title "${GITHUB_REF_NAME}" \
              --notes-file release-notes.md
          fi

      - name: Upload release assets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          gh release upload "${GITHUB_REF_NAME}" release-dist/* --clobber
