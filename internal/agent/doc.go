// Package agent implements an optional agentic reasoning layer that uses a
// local LLM (via Ollama) to plan and chain codegraph tool calls in a ReAct
// (Reason-Act-Observe) loop, synthesizing natural-language answers.
package agent
